[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fermigrator"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "wannierberri",
]
